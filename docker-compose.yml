version: '3'
services:
    app:
        container_name: syrius-api
        restart: always
        build: .
        environment:
            - POSTGRES_HOST=syriusdb.clrbhlis78os.us-east-1.rds.amazonaws.com
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=adot2022
            - POSTGRES_DB=syriusdb
        ports:
            - "3000:3000"